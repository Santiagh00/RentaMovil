package com.rentamovil.model;

/**
 * Roles disponibles en el sistema.
 * ADMIN: acceso completo al panel de administración.
 * USER: acceso limitado (portal de clientes).
 */
public enum Rol {
    ADMIN,
    USER
}
